<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.trainer_name}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment.text}}
    </div>
  </section>
  {{/each}}
</div>